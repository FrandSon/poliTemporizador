{
  "name": "politemporizador",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
    "docker:build": "docker build -t mi-proyecto .",
    "docker:run": "docker run -p 3000:80 mi-proyecto"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
